# Test-specific overrides
# File storage goes into target/ so it is cleaned on next Maven build
app.storage.path=target/test-generations

# Safe non-secret defaults for local test execution only (never deployed to production)
DB_USERNAME=postgres
DB_PASSWORD=postgres
APP_OPENAI_API_KEY=mock-openai-key-for-tests

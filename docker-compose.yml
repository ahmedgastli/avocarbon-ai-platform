services:
  postgres:
    image: postgres:17-alpine
    container_name: avocarbon-postgres
    environment:
      POSTGRES_DB: avocarbon
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    restart: unless-stopped

volumes:
  postgres_data:
